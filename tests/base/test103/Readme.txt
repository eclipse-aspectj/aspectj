Mode:  vm run
Title: a simple test of new fields, methods and constructors

A simple test of new fields, methods and constructors,
using introduce.  This is deprecated as of 0.7, but we
still need to test the deprecated stuff until we kill
it completely. -eh
